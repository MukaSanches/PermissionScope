# Keep verification dependencies registry-only.
# npm 11.15+ can explicitly reject Git, remote-tarball, local-file, and local-directory sources.
# This complements npm ci --ignore-scripts and the committed lockfile without affecting registry packages.
allow-git=none
allow-remote=none
allow-file=none
allow-directory=none
